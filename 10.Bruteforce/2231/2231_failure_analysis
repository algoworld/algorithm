# 문제 분석

해당 문제는 입력되는 값까지 모든 정수들이 해당 값의 생성자인지 판단하는 과정을 거친다

구글링 해결 과정의 핵심은 (1) %10으로 각 자리수를 뽑아내는 것과 (2)if 문을 통해 생성자가 없는경우(조건값과 입력값이 같은 경우)와
생성자가 있는 경우( 문제에서는 여러개일 경우 작은 값이라고 명시해 두었기 때문에 최솟값이 나오면 바로 break를 한다)를 검사한다는 점이다.

특히 가장 쉽게 자리 수를 뽑는 방법을 코드로 기억해두어야 할 것 같다

while (copy) {
			sum += copy % 10;
			copy /= 10;
}
(다음에 써먹자)
